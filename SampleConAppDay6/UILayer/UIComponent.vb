Module UIComponent
    Sub Main()
        Dim frm As New UIForm
        frm.ShowDialog() 'Displays the form as Modal form where the User has to close the form to move to other part of the app.
    End Sub
End Module
